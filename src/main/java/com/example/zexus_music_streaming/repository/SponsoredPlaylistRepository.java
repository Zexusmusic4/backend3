package com.example.zexus_music_streaming.repository;

import com.example.zexus_music_streaming.model.SponsoredPlaylist;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SponsoredPlaylistRepository extends JpaRepository<SponsoredPlaylist, Long> {
}
